# elastic-search-query-script

A simple nodejs based script that calls elastic search, parses the result and writes it to CSV.
It uses ES6 promise.

*ENV Variables*

```
  process.env.ES_URL 
  process.env.ES_USER
  process.env.ES_PASS
```
